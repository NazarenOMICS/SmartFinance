# Stripe Integration

The SaaS billing model starts with:

- `free`
- `pro_monthly`
- `pro_yearly`

Stripe is web-first for checkout and portal, while the backend remains the source of truth for subscription state and usage limits.
