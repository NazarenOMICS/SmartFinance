# Cloudflare Infrastructure

This folder documents production bindings and deployment assumptions for:

- Cloudflare Workers
- D1
- R2
- Cron triggers

The live configuration currently lives in `apps/api/wrangler.toml`.
