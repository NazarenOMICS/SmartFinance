# Scripts

This directory is reserved for operational helpers such as:

- data backfills
- migration verification
- release checks
- import fixtures

The foundation phase keeps it intentionally empty until the first migration scripts are introduced.
